let song = "Tum hi ho";
let score=9
let highestscore=10
let heading=song.charAt(0)
console.log(heading)

// There are 3 methods for extracting a part of a string
let text = "There are 3 methods for extracting a part of a string:"
let part = text.slice(0 , 11)
console.log(part);


const objective=`i like this ${song} i will gave rating ${score/highestscore*100}%`;
console.log(objective)

if(){
    
}